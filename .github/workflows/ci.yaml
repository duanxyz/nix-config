name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint-build:
    name: lint-build (${{ matrix.system }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        system:
          - x86_64-linux
          - aarch64-linux
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            extra-platforms = aarch64-linux

      # NOTE: Memastikan kontributor baru mengenali perintah devshell yang tersedia.
      - name: List devshell commands
        run: nix develop .#default --command devshell commands

      - name: Check formatting
        run: nix develop .#default --command bash -lc "nix fmt && git diff --exit-code"

      - name: Flake checks
        run: nix develop .#default --command bash -lc "nix flake check -L --system ${{ matrix.system }}"

      - name: Build home profile
        if: matrix.system == 'x86_64-linux'
        run: nix develop .#default --command bash -lc "nh home build . -c home-semar -- -L"

      - name: Build system profile
        if: matrix.system == 'x86_64-linux'
        run: nix develop .#default --command bash -lc "nh os build . -H nixos-semar -- -L"
